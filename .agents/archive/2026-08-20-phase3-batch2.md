# react-windows-ui 阶段 3（第二批）

## 范围

- `ProgressBar`
- `TextArea`
- `SliderBar`

## 完成内容

- 实现文件由 `.js` 改为 `.tsx`。
- 补充显式 Props、原生 HTML 属性扩展、ref 类型、公共类型别名和 JSDoc。
- 保留原有 className、状态 class、popup/ticks DOM、range 计算和事件行为。
- 更新发布声明、根入口类型导出、consumer 类型覆盖和基础组件测试。

## 验证

```text
pnpm lint
pnpm typecheck
pnpm test
pnpm format:check
pnpm build
pnpm test:consumer
pnpm --filter react-windows-ui pack --dry-run
```

## 遗留

- 其余公共组件仍为 JS + `.d.ts`，需继续按小批次迁移。
- Sass 仍会报告旧 SCSS `@import` 的 Dart Sass deprecation warning。
