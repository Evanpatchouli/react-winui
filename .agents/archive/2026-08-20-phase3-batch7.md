# 阶段 3 第七批：ImageView TypeScript 迁移

日期：2026-08-20

## 完成内容

- 将 `src/lib/src/components/ImageView/index.js` 迁移为 `index.tsx`，建立显式 `ImageViewProps`。
- Props 基于 `ImgHTMLAttributes<HTMLImageElement>`，补齐图片源、对象适配、wrapper 尺寸/间距/圆角、loading 状态和无参数 load/error 回调类型。
- 保留默认尺寸 `124`、默认 alt `image`、默认 objectFit `cover`、原有 DOM/class、loader 顺序和回调行为。
- 更新 `src/lib/src/components/ImageView/index.d.ts`、根声明入口、consumer 类型样例、格式检查脚本以及 `src/lib/dist` 产物。
- 新增 ImageView 初始加载、load、error 和显式 loading 测试，测试总数达到 32 个。

## 验证结果

- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm test`：通过，32/32
- `pnpm format:check`：通过
- `pnpm build`：通过
- `pnpm --filter react-winui typecheck`：通过
- `pnpm test:consumer`：通过
- `pnpm --filter react-winui pack --dry-run`：通过，包含 ImageView JS/声明产物

## 未处理遗留项

- Dart Sass 旧 `@import` deprecation warning 仍存在。
- docs 构建仍有大 chunk warning。
- 剩余公共组件：`Accordion`、`Alert`、`AppTheme`、`Dialog`、`InputSearchBox`、`Select`、`MenuBar`、`NavBar`、`TableView`。
