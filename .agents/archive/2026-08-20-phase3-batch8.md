# 阶段 3 第八批：AppTheme TypeScript 迁移

日期：2026-08-20

## 完成内容

- 将 `src/lib/src/components/AppTheme/index.js` 迁移为 `index.tsx`，建立显式 `AppThemeProps` 与 `AppThemeScheme`。
- 将颜色配置从历史 `any` 收紧为字符串，保留 `light`、`dark`、`system`、`current` 方案和可选回调。
- 保留 `React.memo`、初次渲染主题设置、scheme/color 变化时的副作用与回调时序；未修改 DOM 输出、样式或公共 API。
- 更新 `src/lib/src/components/AppTheme/index.d.ts`、根声明入口、consumer 类型样例、格式检查脚本以及 `src/lib/dist` 产物。
- 新增 AppTheme 主题、颜色变量和回调测试，测试总数达到 33 个。

## 验证结果

- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm test`：通过，33/33
- `pnpm format:check`：通过
- `pnpm build`：通过
- `pnpm --filter react-winui typecheck`：通过
- `pnpm test:consumer`：通过
- `pnpm --filter react-winui pack --dry-run`：通过，包含 AppTheme JS/声明产物

## 未处理遗留项

- Dart Sass 旧 `@import` deprecation warning 仍存在。
- docs 构建仍有大 chunk warning。
- 剩余公共组件：`Accordion`、`Alert`、`Dialog`、`InputSearchBox`、`Select`、`MenuBar`、`NavBar`、`TableView`。
