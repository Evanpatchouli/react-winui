# 阶段 3 第十二批：InputSearchBox TypeScript 迁移

日期：2026-08-20

## 完成内容

- 将 `src/lib/src/components/InputSearch/InputSearchBox/index.js` 迁移为 `index.tsx`。
- 新增 `InputSearchSuggestion` 与 `InputSearchBoxProps`，基于 `InputHTMLAttributes<HTMLInputElement>` 明确原生输入、suggestion、宽度、tooltip、事件和 ref 类型。
- 保留默认 placeholder、suggestion 过滤、`show` class、tooltip wrapper、原有 input/ul DOM 结构和 class；未修改视觉 CSS 或公共运行时行为。
- 更新 `index.d.ts`、根入口声明、consumer 类型样例、format 脚本和 `src/lib/dist` 产物。
- 新增 InputSearchBox suggestion 过滤、列表状态、点击回调、change/ref 转发和 disabled 测试，测试总数达到 41 个。

## 验证结果

- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm test`：通过，41/41
- `pnpm format:check`：通过
- `pnpm build`：通过
- `pnpm --filter react-windows-ui typecheck`：通过
- `pnpm test:consumer`：通过
- `pnpm --filter react-windows-ui pack --dry-run`：通过，包含 InputSearchBox JS/声明产物

## 未处理遗留项

- Dart Sass 旧 `@import` deprecation warning 仍存在。
- docs 构建仍有大 chunk warning。
- 剩余公共组件：`Select`、`MenuBar`、`NavBar`、`TableView`。
