# 阶段 3 第十五批：剩余组件与运行链路 TypeScript 迁移

日期：2026-08-21

## 完成内容

- 将 `NavBar`、`NavBarLink`、`NavBarSubMenu`、`NavBarThemeSwitch`、`NavPageContainer` 的真实实现从 `.js` 迁移到 `.tsx`，补齐 public Props、主题类型和 `NavBar` DOM ref 类型。
- 保留 NavBar 的响应式折叠、mobile overlay、滚动阴影、链接 active/badge/image、子菜单高度、主题切换、页面容器 class/style 和滚动行为；未修改 SCSS、颜色、圆角、阴影 recipe 或 DOM 层级。
- 将 `TableView` 迁移到 `.tsx`，新增 `TableViewCell`、`TableViewRow`、`TableViewColumn`、`TableViewProps`；保留列排序、二次点击恢复、排序图标、不可排序 class、header/footer slot 和字体样式。
- 将未被现行入口引用的 `MenuBar/Menu/SubMenu.js` 迁移为带显式 Props 的 `SubMenu.tsx`，并补齐 `MenuList` legacy `listIndex` 类型边界；没有把该 helper 接回活动入口。
- 将组件运行链路的 `Appearance`、`ScrollView`、`getScreenOffset`、API barrel、`useOutSideClick`、hooks barrel、`LoaderBusyWrapper` 和 library root entry 迁移到 TS/TSX；Vite library entry 与 workspace path 改为 `src/lib/src/index.ts`。
- 保持 Alert/Dialog 的 compound component 类型与 TS 实现导出一致，修正 root TS entry 切换后暴露出的 `AlertComponent`/`DialogComponent` 类型缺口。
- 更新 consumer 类型样例、format 脚本、root public type exports 和 `src/lib/dist` ESM/声明产物。
- 新增 NavBar、NavBarLink、NavBarSubMenu、NavBarThemeSwitch、NavPageContainer、TableView 的渲染/交互测试；单元测试总数达到 55 个。

## 验证结果

- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm test`：通过，55/55
- `pnpm format:check`：通过
- `pnpm --filter react-winui typecheck`：通过
- `pnpm test:consumer`：通过，consumer typecheck/build 成功
- `pnpm build`：通过，library/docs production build 成功
- `pnpm --filter react-winui pack --dry-run`：通过，产物包含 public declarations、ESM、CSS 和资源
- `src/lib/src` JS/JSX 扫描：无残留

## 未处理遗留项

- Dart Sass 旧 `@import` deprecation warning 仍存在。
- docs 构建仍有大 chunk warning。
- `src/lib/dist` 保留编译后的 `.js` ESM 产物，这是发布格式，不是未迁移源码。
