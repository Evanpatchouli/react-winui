# 阶段 3 第十四批：MenuBar TypeScript 迁移

日期：2026-08-20

## 完成内容

- 将 `src/lib/src/components/MenuBar/index.js`、`Menu/MenuItem.js`、`Menu/MenuList.js` 迁移为 TSX。
- 新增 `MenuBarHandle`、`MenuBarProps`、`MenuBarComponent` 以及 Item/Divider/SubMenu compound API 类型，明确 imperative ref、anchor ref、ReactNode children 和无参 item callback。
- 保留 open/close、anchor 定位、菜单方向、reverse/backdrop blur、outside click、submenu show class、Divider 和 item click 行为；未修改正常 DOM/class 或视觉 CSS。
- 更新 `index.d.ts`、根入口声明、consumer 类型样例、format 脚本和 `src/lib/dist` 产物。
- 新增 MenuBar imperative ref、定位 class、Divider、item click、submenu toggle 和 outside click 测试，测试总数达到 45 个。
- `MenuBar/Menu/SubMenu.js` 未被当前入口引用，作为遗留死文件暂未迁移并已记录。

## 验证结果

- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm test`：通过，45/45
- `pnpm format:check`：通过
- `pnpm build`：通过
- `pnpm --filter react-winui typecheck`：通过
- `pnpm test:consumer`：通过
- `pnpm --filter react-winui pack --dry-run`：通过，包含 MenuBar JS/声明产物

## 未处理遗留项

- Dart Sass 旧 `@import` deprecation warning 仍存在。
- docs 构建仍有大 chunk warning。
- `MenuBar/Menu/SubMenu.js` 仍为未接入的 legacy helper。
- 剩余公共组件：`NavBar`、`TableView`。
