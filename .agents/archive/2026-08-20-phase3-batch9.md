# 阶段 3 第九批：Alert TypeScript 迁移

日期：2026-08-20

## 完成内容

- 将 `src/lib/src/components/Alert/index.js` 迁移为 `index.tsx`。
- 新增 `AlertHandle`、`AlertProps`、`AlertSlotProps` 与 `AlertComponent`，明确 imperative ref 和 `Header`/`Footer` 静态成员类型。
- 保留 alert wrapper/modal DOM、class、标题/消息、children、遮罩点击、滚动锁定和历史 class 拼写。
- 使用内部 DOM ref 保持 forwarded ref 的 `open`/`close` handle，未改变外部 imperative API。
- 更新 `src/lib/src/components/Alert/index.d.ts`、根声明入口、consumer 类型样例、格式检查脚本以及 `src/lib/dist` 产物。
- 新增 Alert 内容/slot、imperative visibility、backdrop click、controlled visibility 测试，测试总数达到 35 个。

## 验证结果

- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm test`：通过，35/35
- `pnpm format:check`：通过
- `pnpm build`：通过
- `pnpm --filter react-windows-ui typecheck`：通过
- `pnpm test:consumer`：通过
- `pnpm --filter react-windows-ui pack --dry-run`：通过，包含 Alert JS/声明产物

## 未处理遗留项

- Dart Sass 旧 `@import` deprecation warning 仍存在。
- docs 构建仍有大 chunk warning。
- 剩余公共组件：`Accordion`、`Dialog`、`InputSearchBox`、`Select`、`MenuBar`、`NavBar`、`TableView`。
