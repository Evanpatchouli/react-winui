# 阶段 3 第十三批：Select TypeScript 迁移

日期：2026-08-20

## 完成内容

- 将 `src/lib/src/components/SelectMenus/Select/index.js` 迁移为 `index.tsx`。
- 新增 `SelectOption`、`SelectOptionValue`、`SelectChangeHandler` 与 `SelectProps`，明确 custom select 的 option、default value 和 value change API。
- 保留 trigger、tooltip、默认选中项、selected/show/reverse/backdrop blur class、外部点击关闭、滚动锁定和原有 DOM/class；未修改视觉 CSS。
- 对空 items 和无效 defaultValue 增加安全回退，避免读取不存在 option；正常数据行为保持不变。
- 更新 `index.d.ts`、根入口声明、consumer 类型样例、format 脚本和 `src/lib/dist` 产物。
- 新增 Select 默认选中、菜单开关、选项选择、回调、滚动锁定、custom trigger 和外部点击测试，测试总数达到 43 个。

## 验证结果

- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm test`：通过，43/43
- `pnpm format:check`：通过
- `pnpm build`：通过
- `pnpm --filter react-windows-ui typecheck`：通过
- `pnpm test:consumer`：通过
- `pnpm --filter react-windows-ui pack --dry-run`：通过，包含 Select JS/声明产物

## 未处理遗留项

- Dart Sass 旧 `@import` deprecation warning 仍存在。
- docs 构建仍有大 chunk warning。
- 剩余公共组件：`MenuBar`、`NavBar`、`TableView`。
