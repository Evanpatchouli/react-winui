# 阶段 3 第十一批：Accordion TypeScript 迁移

日期：2026-08-20

## 完成内容

- 将 `src/lib/src/components/Accordion/index.js` 迁移为 `index.tsx`。
- 新增 `AccordionProps`、`AccordionSlotProps` 与 `AccordionComponent`，明确 Trigger/Body compound slots、样式和展开回调。
- 保留 accordion root/header/body DOM、class、`aria-expanded`、headerTitle fallback、panel height、resize listener 与 callbacks。
- 使用 `isValidElement`/displayName 检查筛选 Trigger/Body，使用 `HTMLElement` 收窄读取 `clientHeight`，未引入 `any`。
- 更新 `src/lib/src/components/Accordion/index.d.ts`、根声明入口、consumer 类型样例、格式检查脚本以及 `src/lib/dist` 产物。
- 新增 Accordion slot、headerTitle fallback、样式和展开/收起测试，测试总数达到 39 个。

## 验证结果

- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm test`：通过，39/39
- `pnpm format:check`：通过
- `pnpm build`：通过
- `pnpm --filter react-winui typecheck`：通过
- `pnpm test:consumer`：通过
- `pnpm --filter react-winui pack --dry-run`：通过，包含 Accordion JS/声明产物

## 未处理遗留项

- Dart Sass 旧 `@import` deprecation warning 仍存在。
- docs 构建仍有大 chunk warning。
- 剩余公共组件：`InputSearchBox`、`Select`、`MenuBar`、`NavBar`、`TableView`。
