# 阶段 3 第十批：Dialog TypeScript 迁移

日期：2026-08-20

## 完成内容

- 将 `src/lib/src/components/Dialog/index.js` 迁移为 `index.tsx`。
- 新增 `DialogHandle`、`DialogProps`、`DialogSlotProps` 与 `DialogComponent`，明确 imperative ref、modal style 和 Body/Footer 静态成员类型。
- 保留 dialog wrapper/modal DOM、class、backdrop blur、遮罩点击、ScrollView 锁定和 Body/Footer 渲染行为。
- 使用内部 DOM ref 保持 forwarded ref 的 `open`/`close` handle，未改变外部 imperative API。
- 更新 `src/lib/src/components/Dialog/index.d.ts`、根声明入口、consumer 类型样例、格式检查脚本以及 `src/lib/dist` 产物。
- 新增 Dialog slot、样式、imperative visibility、backdrop click 和 controlled visibility 测试，测试总数达到 37 个。

## 验证结果

- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm test`：通过，37/37
- `pnpm format:check`：通过
- `pnpm build`：通过
- `pnpm --filter react-winui typecheck`：通过
- `pnpm test:consumer`：通过
- `pnpm --filter react-winui pack --dry-run`：通过，包含 Dialog JS/声明产物

## 未处理遗留项

- Dart Sass 旧 `@import` deprecation warning 仍存在。
- docs 构建仍有大 chunk warning。
- 剩余公共组件：`Accordion`、`InputSearchBox`、`Select`、`MenuBar`、`NavBar`、`TableView`。
